<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jonat.kresz" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TesztActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".TemakorActivity"
            android:label="@string/title_activity_list" />
        <activity
            android:name=".FeladatActivity"
            android:label="@string/title_activity_feladat" />
        <activity
            android:name=".KerdesActivity"
            android:label="@string/title_activity_kerdes" />
        <activity
            android:name=".Vege"
            android:label="@string/title_activity_vege" >
        </activity>
    </application>

</manifest>
